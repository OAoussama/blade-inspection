# Read by the FastAPI app (app/config.py)
# Must match the values in the root .env used by docker-compose.

DATABASE_URL=postgresql://postgres:changeme@localhost:5433/blades

# Optional — overrides the default set in app/config.py
MODEL_REVISION=v2
